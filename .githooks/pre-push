#!/usr/bin/env bash
# Reject the push if lint or build fails (same checks as GitHub CI).
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"

while read -r _local_ref _local_sha _remote_ref _remote_sha; do
  :
done

npm run lint
npm run build
